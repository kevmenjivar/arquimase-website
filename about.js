async function loadAbout(){try{const response=await fetch('/api/settings');const data=await response.json();const settings=data.settings||{};if(settings.about_title)document.querySelector('#about-title').innerHTML=settings.about_title;if(settings.about_text)document.querySelector('#about-text').textContent=settings.about_text;if(settings.about_image)document.querySelector('#about-image').style.background=`url('${settings.about_image}') center/cover no-repeat`}catch{}}loadAbout();
